using System;
using System.Threading;

namespace CelsiusToFahrenheit
{
    class Program
    {
        static void Main(string[] args)
        {
            //Напишете програма, която познава дали е топло или студено навън.
            //От конзолата се чете един ред – текст, който подсказва какво е времето.
            //При въвеждане на "sunny" трябва да се отпечата "It's warm outside!".
            //Във всички останали случаи (cloudy, snowy)трябва да се отпечата "It's cold outside!".
            //
            //1. Въвеждаме променлива тип string (низ) и я четем от конозлата
            string weather = Console.ReadLine();

            //2. Сравняваме дали низът е идентичен на sunny и отпечатваме на конзолата
            if(weather == "sunny")
            {
                Console.WriteLine("It's warm outside!");
            }
            else
            {
                Console.WriteLine("It's cold outside!");
            }
        }
    }
}